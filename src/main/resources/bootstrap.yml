spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: multimedia-service
  cloud:
    config:
      uri: http://localhost:8880
      fail-fast: false
rugid:
  images:
   folder-path: "content"